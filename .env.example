# Embedding Configuration
EMBEDDING_BACKEND=openai  # or "local" for sentence-transformers
OPENAI_API_KEY=your-api-key-here  # Required if using EMBEDDING_BACKEND=openai

# Database Configuration (for local development)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/consultant_matching

# Redis Configuration (optional, for caching)
REDIS_URL=redis://localhost:6380

# Playwright MCP Configuration (for authenticated scrapers)
PLAYWRIGHT_ENABLED=false  # Set to true to enable Playwright-based scrapers
PLAYWRIGHT_PROXY=  # Optional: socks5://user:pass@host:port or http://proxy:port

# Cinode Credentials (required if PLAYWRIGHT_ENABLED=true)
CINODE_USERNAME=your-cinode-email@example.com
CINODE_PASSWORD=your-cinode-password

# LinkedIn Credentials (required if PLAYWRIGHT_ENABLED=true)
LINKEDIN_USERNAME=your-linkedin-email@example.com
LINKEDIN_PASSWORD=your-linkedin-password

# Docker will use internal service names, but for local development use these ports:
# - PostgreSQL: localhost:5433
# - Redis: localhost:6380
# - API: localhost:8001
# - Playwright MCP: localhost:8931